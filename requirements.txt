# SceneSplat — Python dependencies (Windows-native, verified)
#
# Environment: Python 3.10, PyTorch 2.5.1, CUDA 12.4, Windows 11 + MSVC 2022
# Verified with the standalone inference pipeline:
#   1. scripts/preprocess_gs.py    (PLY -> npy)
#   2. tools/lang_inference.py     (PT-v3 forward -> feat.pt)
#   3. scripts/pca_colorize_features.py  (feat.pt -> PCA PLY)
#
# Install order:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
torch-scatter
 torch-sparse
  torch-cluster
#   # CUDA extensions (need cl.exe on PATH, do NOT run inside vcvars64 shell):
./libs/pointops
./libs/pointgroup_ops

# ---- Core deep learning ----
torch==2.5.1
torchvision==0.20.1
spconv-cu124          # PT-v3 sparse convolution backend

# ---- Point cloud / geometry ----
open3d==0.19.0
plyfile==1.1
einops==0.8.1

# ---- Scientific ----
numpy<2.5
scipy==1.15.2
numba==0.61.2
h5py

# ---- PyTorch Geometric (required by simdinov2 model import) ----
torch-geometric

# ---- Vision-language ----
transformers==4.50.1
timm
ftfy
regex

# ---- Config / logging / utils ----
PyYAML
tensorboardX
tensorboard
wandb
addict
yapf
black
termcolor
tqdm
matplotlib==3.10.3

# ---- Build tools ----
ninja

# ---- Optional: tokenizer for label encoding ----
git+https://github.com/openai/CLIP.git

# ---- NOT required on Windows ----
# SharedArray==3.2.4     # POSIX-only (sys/mman.h); cache.py degrades gracefully
# flash-attn             # config sets enable_flash=False
ocnn                   # only needed for OctFormer model, not PT-v3
